export enum TableType {
  Basic = "basic",
  // TODO: add other table types
}

export const DefaultPaginationSizeOptions = [10, 20, 50, 100];

export const DefaultResizeOptions = { min: 100, max: 800 };

export const DefaultVirtualizationRowBuffer = 10;
